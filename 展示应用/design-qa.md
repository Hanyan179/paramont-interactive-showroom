## 2026-09-10 首页取景与旋转

已修复选中主体随拖动出框及公司世界横向角度限制。取景跟随模型的世界坐标和可见网格外轮廓，避让现有正文、场景说明及导航；横向连续旋转，保留俯仰上下限及返回方向。22 项测试与构建通过；浏览器检查与截图见 [本轮记录](qa/home-framing/验收记录.md)。

## 本轮：文档阅读、跨区状态和正确标志

[验收记录](qa/document-reader/验收记录.md) · 四区图文阅读、相册、透明原色标志和工厂残留修复。19 项主应用测试与 4 项产品测试通过，实机触摸待验收。

# 2026-09-10 场景多样性与焦点精修

最新交付见 [本轮评审与改进](qa/refinement-70/评审与改进.md)。用户最新要求为场景形态变化、一次一个视觉主体，覆盖本轮中途增加四组对象同时陈列的方案。

# 统一流动空间与无人展示 · 本轮检查

本轮实现与下列浏览器检查完成；视觉仍供用户审阅，不代表已达到概念图的模型精细度或已通过实屏验收。上一轮报告保存于 `qa/material-light-design-qa-before-space-flow.md`。

## 设计落实

- 白色阅读区和深蓝模型区合为连续深蓝空间。云带、疏星、低位雾光共用同一套实时背景；正文改为柔和浅色，玻璃采用深色半透明，模型保留银灰、品牌蓝、岩石与暖金属。保留现有界面结构与四区入口。
- 静置时背景持续缓慢变化，连接光沿原路径移动，首页地球轻微转动，镜头有小幅呼吸位移。按下模型后收敛镜头运动；隐藏时暂停场景时间，恢复时不追赶墙钟时间。
- 空闲默认 90 秒进入慢速导览。公司总览、公司聚焦、连接世界、全球商品流、三地团队、产品世界、数据工作站依次推进，10 个镜头共约 174 秒，每镜 12–26 秒。产品模块内每镜 10 秒，由父展厅控制进入与退出。
- 全局只保留一个展示提示和一个接管遮罩。首次触摸恢复原来的选择、详情、产品目录与搜索条件；明确点击全局导航则进入目标页面。拖动不会误选入口，按住触点、播放视频、页面隐藏阻止无人计时触发。
- 使用官方白色标志文件保留山形细节。仅用显示裁切消化原图片透明留边，不重画标志。修复英文地球长标题与下部操作区的旧间距问题。
- 静态图片或档案长时间无人查看也可进入导览，退出恢复原有内容；手动视频仍单独阻止空闲触发。嵌入模块隐藏时清除父页面的播放阻塞状态。

## 同尺寸视觉对照

`qa/space-flow-comparison.png` 同屏比较两张 1920×1080 中文首页总览：左侧为 `material-light-home-1920.png`，右侧为 `space-flow-home-1920.png`。两图等比缩放，未裁掉界面。

已查看合并对照：白蓝分割消失，文字及底栏与模型处于同一环境；总览、导航、模型角色保持原有构图。修正了初版标志简单变白导致山形丢失的问题。右图地球角度与连接光位置因实时运动不同，不作为静态像素一致性目标。

材质精细度仍有边界：桌面与数据设备沿用原模型，底座形态尚未逐个精修；本轮主要解决整体环境与运动统一，不能写成四区模型全部升级或等同生成概念图。

其他画面：`space-flow-home-1524.png`、`space-flow-globe-en-1920.png`、`space-flow-globe-en-1366.png`、`space-flow-insight-presentation.png`。`space-flow-home-motion.png` 是同场景另一时刻的记录，证明应以动态预览判断，不能把单帧截图当作连续运动验收。

## 实际浏览器路径

使用内置浏览器独立检查页，未操作用户的原浏览状态。

- 1366×768：实际坐标拖动山体区域，松开后仍是首页总览，四个对象均未选中，没有误触跳转。
- 首页选中地球 → 自动展示 → 点击结束：回到地球选中状态，保留进入按钮，没有点穿进入全球页。
- 工厂能力 → 中国 → 走进创研空间 → 自动展示 → 直接点击数据分析：目标数据场景可见，旧工厂详情没有残留。
- 产品目录搜索“彩环” → 自动展示 → 跨区域推进。记录第 40 秒为连接世界，第 70 秒为中国，第 86 秒为柬埔寨，第 106 秒为美国，第 141 秒已到数据工作站，未卡在产品区。退出后恢复产品目录及单个“层叠彩环”结果、3 款式。没有扩大品牌内部业务测试。
- 无人触发：重新加载公司首页后不再输入，第 13、48、76 秒仍为 interactive，第 100 秒为 playing / company-world；默认 90 秒阈值另外由确定性状态测试覆盖。
- 全高清：1920×1080 首页与英文地球页面完整查看，正文、操作区域、底栏无新增遮挡。英文地球文档宽度读数为 1920。
- 小桌面：1366×768 英文地球标题、说明及两组选择与底栏均可见。中文数据工作站画面亦查看。
- 较窄比例：1524×1217 首页查看，模型、标题、底栏均在画面内。
- 超高清：3840×2160 数据页只做布局边界读取，文档宽度 3840，底栏位于 x=134.39、y=1954.81、w=3571.22、h=157.67。没有把这个读数当成完整 4K 视觉或实屏验收。
- 最后浏览器警告及错误列表为空。

## 构建与逻辑验证

主应用和产品模块构建成功，产品模块已重新导出到主应用本地静态目录。没有新增生产依赖、远程接口或外部发布。

9 项测试通过：5 项覆盖 90 秒阈值、输入重置、持续按住/视频/隐藏阻塞、避免恢复时连续追赶镜头、接管与状态恢复、四区导览循环；4 项覆盖原有静态交付服务。最终三维公共包约 582 KB，仍有构建体积提示，不是构建失败。

## 尚未验证

- 真实触摸屏、多点触摸、实际电脑的性能与 30 分钟连续运行。
- 原生视频在真实浏览器上的完整播放、拖动、全屏路径；目前覆盖父子播放桥接、隐藏清理代码及导览阻塞逻辑测试，不能等同完整媒体验收。
- 4K 实际渲染细节、所有业务空间与每个品牌页面的完整回归。

主预览仍使用 5183 端口。此报告只针对本轮背景、持续运动和导览控制，不宣称原阶段计划整体完结。

## 2026-09-14 最新验收：品类层级与平面供应链地图

本轮将六个品类选择收进左侧内容，保留底部统一主导航；供应链采用斜俯视平面地图、陆块厚度、蓝色海岸光和金色协作线，突出中国核心节点。三地模型入口、返回、筛选、搜索、地图拖动与品类切换均已实际验证。90 项测试和构建通过，1920×1080、1366×768、1280×960 对应页面检查通过。

参考、最终截图、修复项和验证边界见 [本轮验收记录](qa/category-supply-20260914/验收记录.md)。3840×2160 仅有布局边界验证，完整超高清捕获和物理大屏验收未完成。上述历史报告保留其当时范围，本轮最终方案以平面地图和现有五个主题为准。


## 2026-09-14 品类方案 2 · 竖向弧形展窗

六张竖版品类图已生成并接入，六窗同屏，支持靠近、左右切换、阅读原档案与返回；主导航位置固定。方案 1 通过查询参数保留。参考图与同尺寸 1474×856 总览已在同一次图像输入中对照，首轮遮挡、近景取景和暂停后点击区域问题均已修复复核。

完整范围、截图、交互与验证边界见 [本轮验收记录](qa/category-portals-v2/验收记录.md)。93 项测试、内容检查与构建通过；实际检查 1920、1366 和 1024 桌面尺寸。3840 仅有布局边界与取景测试，截图捕获异常，尚未进行物理大屏验收。

final result: passed

## 2026-09-14 品类方案 2 · 明暗色系与材质精修

已根据阴暗、边框和字体反馈接入明暗两版品类展廊：六张日光竖图、两套同构图建筑背景、细边框、清晰名称与配色同步的操作区域。默认明色，右上切换；近景与色系状态保留，原产品模型场景继续使用原样式。

参考图与明暗总览、明暗近景在同一次图像输入中对照；首次发现的厚边框、平铺感反射、偏暗产品与底栏对比问题已调整复核。实际操作检查色系、语言、进入/返回、原档案、原模型及跨页导航位置。94 项测试、内容检查和最终构建通过。

截图、修复记录与验收边界见 [本轮记录](qa/category-palettes-v3/验收记录.md)。1024、1366、1474、1920 桌面画面已检查；3840 仅有布局和取景验证。明暗方案仅本轮品类展廊范围，未声称全站或物理大屏验收。

final result: passed

## 2026-09-14 品类背景统一 · 最新采用版本

用户指出建筑背景与整体不协调，因此上一节暖色建筑背景的采用结论被本轮替代。品类现在共用展厅的连续流动空间，默认深蓝，保留银蓝明色；名称底色、边缘和环境光同步调整。六张明亮产品竖图和原交互保持。

同尺寸旧版与新版截图已对照，实际检查银蓝 → 品牌 → 品类的配色恢复、近景返回、中英文及持续动画。94 项测试、内容检查及最终构建通过，浏览器无错误/警告。详细截图与边界见 [本轮记录](qa/category-harmony-v4/验收记录.md)。

final result: passed

## 2026-09-14 品类边框与图文接缝 · 用户复核后的修正

用户继续指出边框、清晰度和整体视觉差距。此前的通过结论仅覆盖当时记录的检查，不代表用户认可或参考图整体风格达标。

本轮替换闭合实体框，合并图文展示面，增大名称并关闭品类泛光，修复近景地面穿过展窗的横带。实际检查中英文、明暗切换、进入/返回和固定导航位置；94 项测试、内容检查与最终构建通过。

与参考图对照后，仍保留 P1 视觉差距：产品陈列图没有形成完整生活场景的纵深与建筑照明。不能因工程检查通过而宣称整体视觉验收通过。详见 [修复证据与未达标项](qa/category-surfaces-v5/验收记录.md)。

final result: needs revision


## 2026-09-15 用户确认星海首页并合并主版本

最新采用版本为原始品牌标志装置、动态星空与流动水面；品类恢复横向展示。品牌透明底清理、越南供应链与地图定位更新独立保留。此前品类展窗试验的采用结论不适用于当前版本。合并及验证范围见 [本地合并记录](docs/2026-09-15-首页版本合并.md)。

## 2026-09-15 字体、导航与地图留白精修

本轮优化地图标题、国家说明、数据区与五主题导航的字体及间距；修正旧导航高度导致的数据重叠、窄窗口工具遮挡与地区入口溢出。保留用户确认的首页、水面及星海背景，并同步到首页改版工作树。

五种地图视口、中英文和五个主题总览已实际查看，98 项测试、内容检查和构建通过。完整范围及截图见 [精修验收记录](../design-qa.md)。物理大屏和触摸验收不在本轮验证结论内。


## 2026-09-15 横向触摸切换主题

- 公司页、外层页面空白区域与底部主题导航支持单指左滑到下一主题、右滑到上一主题；首尾循环。鼠标拖动保持现有场景操作。
- 沿用 scenePointer 的捕获、拖动分类和兼容点击拦截；滑动在松手时确认，64–150 CSS 像素自适应阈值，横向距离至少为纵向的 1.6 倍。多指、取消、捕获丢失、页面隐藏、打开详情或阅读层会取消待完成的切页。
- 地图、品牌行、品类浏览和三维详情保留各自拖动；底栏提供跨主题滑动入口。
- 在 Chrome 通过浏览器原生触摸模拟验证公司左滑到全球供应链、底栏左滑依次到品牌和品类、右滑返回公司、底栏轻点、纵向滑动与多指不切页。地图缩放后横向平移约 250 CSS 像素时保持供应链主题；品牌行改变位置，品类从 KIDS CRAFT 切到 STATIONERY 时保持品类主题；中国详情横滑改变相机位置且保持 china 深度。浏览器未记录错误或警告。
- npm test：品牌模块 4 项、展示应用 103 项全部通过，内容校验通过。Sites 构建脚本调用完整根构建成功，既有大资源包提示仍在。首次构建命中系统 Node 签名兼容问题，指定现有 Node 24 PATH 后通过，未修改依赖。
- 当前局域网预览仍为 http://192.168.103.9:5183/。未发布。浏览器触摸模拟不代表真实平板或电视接屏验收。

## 2026-09-19 智能与洞察 · 实时三维主体

用户否定了图片主体：六阶段必须由代码塑造并持续运转。本轮保留全局总览、阶段详情、业务示例三层结构，替换主体实现；旧 intelligence-v2 六张主体图与案例图不再由页面加载。

- `intelligenceArtifacts.js` 提供有厚度的玻璃方块、银蓝扭曲带分析球、暖金核心晶体、三层动态玻璃图表和知识方块。`intelligenceGlobe.js` 使用真实球体与既有地球表面/法线贴图，仅属于智能场景，不恢复供应链球视图。
- 同一组 2400 粒子及 108 个实体晶体片段持续重组；全局知识回流沿下方弧线返回。六个大型实体主体仍各自保留并通过材质显隐衔接，连续形变发生在共享粒子/晶体结构，不能描述为所有实体网格拓扑直接互变，也不是真实流体物理。
- 业务示例改为实际几何：三张分析面板、积木、堆叠环和彩铅。内容仍明确标记场景示意，未接入实时数据。
- 详情拖动直接旋转三维主体、固定相机与文字导航；拖动后未误进案例。手动选择停止阶段推进，返回案例保留所选阶段。暂停后相隔检查运动时钟保持一致。六入口与主体间空白区点选保护通过。

浏览器实查：1920×1080 总览/详情，1366×768 英文详情，1200×900 中英文案例；发现窄屏案例底座裁切，调整比例和中心后重新加载并截图确认完整。3840×2160 的 DOM 与绘制缓冲区均为实际 4K，取景/点选单测通过；工具的 4K 截图输出存在裁切，不以该截图声称整幅视觉验收。最终浏览器未记录运行错误。

- [真实模型总览](docs/qa/intelligence-flow/models-global-1920.jpg)
- [真实分析模型详情](docs/qa/intelligence-flow/models-detail-1920.jpg)
- [窄屏案例修正后](docs/qa/intelligence-flow/models-case-narrow.jpg)

根目录 `npm test`：主展厅 131 项、品牌模块 4 项通过，内容检查通过。根目录 `npm run build` 通过；既有超过 500 kB 的资源包提醒仍在。无新增依赖。CPU 更新只读基线：1920×1080 四状态 P95 为 0.346–0.470 ms，114 个独立几何体和 62 个材质引用保持稳定；该数值不含 GPU 渲染，不能替代大屏实机帧率。

本轮结论：实现与本地交互检查完成，视觉采用仍待用户查看；未进行物理触摸屏验收，未发布。


## 2026-09-19 · 地球主视觉与产品资料形态精修

### 本轮目标与来源

- 最新用户反馈优先：总览不再并排堆叠六种模型，保留用户认可的地球；删除含义不清的方块，结合产品图片。
- 同视口比较基线：`docs/qa/intelligence-flow/models-global-1920.jpg`（上一版浏览器实拍，1920×1080）。本轮与 `refined-global-1920.jpg` 在同一比较输入中打开检查。
- 早期六阶段参考图只保留银蓝材质、暗背景与三层探索关系；其六模型横排已被本轮用户要求明确取代，不作为布局复刻标准。
- 产品来源：`共享数据/featured-categories.json` 中既有的 kids-craft、beauty、toys 高清示意图，1536×1024；复用原图及原去底阈值，没有生成新的产品图片。它们是用户此前选择的展厅示意素材，不是真实 SKU 实拍。页面和素材模块均保留示意边界。

### 实现与检查

- 总览只展示一个大型地球，三处产品图使用同一三维坐标空间中的金属托片，有深度遮挡、反光和轻微浮动。六个阶段入口固定在页面上，模型本身不承担导航布局。
- 数据资产改为带产品图、材料色样与资料页结构的三维薄片；智能进化改为带册页、装订边与书签的知识档案。原来的数据立方体阵列、知识方块堆叠不再实例化。
- 地球、薄片、托片、光流均为实际几何体；产品局部使用图像材质。主模型过渡仍为渐隐/渐显与共享粒子位置插值，不宣称任意主网格已经实现统一拓扑变形。
- 几何体、纹理、粒子缓冲区在导航与循环中保留，全部图片进入共享加载管理器，纹理可由父场景统一释放。没有引入独立动画循环或新依赖。

### 视觉迭代与证据

1. [P1，已修复] 首轮图片的黑色底遮住球面。采用现有品类展厅的线性色彩去底阈值，保留轮廓，关闭黑色矩形遮挡。后续 1920 / 1366 / 1200 画面已检查。
2. [P2，已修复] 无限制自转使地球长时间只显示海洋，失去识别性。改为围绕大陆正面的连续缓慢摆转，保留球面材质与高光。
3. [P2，已修复] 资料页静止时粒子与碎片形成杂点。降低停留状态粒子强度，碎片只在形态重组期间显示；持续知识点收回资料册内部。`refined-assets-1920.jpg`、`refined-knowledge-1920.jpg` 为修正后画面。
4. [P2，已修复] 1366×768 阶段入口与全站导航靠得过近。短屏单独收紧阶段按钮高度并上移该行，保留触摸区域。修正后 `refined-global-1366.jpg` 已重新截取检查。

五项视觉检查：
- 字体：沿用当前展厅字体，中文标题和英文标题均无截断；中英文阶段名称在窄屏可读。
- 间距：左侧说明与右侧主视觉分开；1920、1366、1200 三种尺寸中图像、阶段入口与底部导航无文字遮挡。
- 色彩：保留银蓝地球、少量金色轨道与既有暗色背景；产品图维持原素材颜色，不做全屏着色。
- 图像：使用完整高清本地图，图片仅承载产品内容；近看边缘不再有黑色矩形，模型具有实际体积。
- 内容：资料页说明与知识归档说明已对应新形态；未新增业务指标、品牌归属或真实市场结果。

完整证据：
- `docs/qa/intelligence-flow/refined-global-1920.jpg`
- `docs/qa/intelligence-flow/refined-global-1366.jpg`
- `docs/qa/intelligence-flow/refined-global-narrow-en.jpg`（1200×900）
- `docs/qa/intelligence-flow/refined-assets-1920.jpg`
- `docs/qa/intelligence-flow/refined-knowledge-1920.jpg`

截图按 CSS 视口 1:1 获取。总览新旧均为 1920×1080，不作拉伸；模型边缘和资料页已通过单独详情视角检查。4K 仅验证 3840×2160 DOM 布局、六入口范围与几何拾取，不宣称本轮完成了完整 4K 截图或实体触摸屏验收。

### 功能与工程验证

- 浏览器：六阶段入口可选；点击地球进入价值实现；拖动模型保持详情，不误开案例；资料阶段到知识阶段、进入案例、返回同阶段、回到总览、切换中英文、暂停/恢复均已操作检查。
- 1200×900 英文及 3840×2160 中文：六个阶段按钮均在视口内，无横向溢出。
- 最终完整刷新之后的浏览器错误为零。修改期间旧组件热更新产生过一次旧布局字段错误，完成组件更新并完整刷新后没有再出现。
- 根目录 `npm test` 通过（主应用 131 项、嵌入模块 4 项，内容检查通过）；根目录 `npm run build` 通过。最后一次短屏 CSS 调整后，主应用构建再次通过。保留既有大分包体积警告。
- 28 项智能场景专项测试覆盖单一总览、六详情入口、示例返回、共享物质连续性、四种尺寸的拾取、真实几何和允许的产品图、资源归属及减少动态偏好。

final result: passed

此结论表示本轮浏览器设计与功能检查通过。整体审美仍待用户看动态预览确认；真实产品实拍和实体大屏的最终显示效果未纳入本轮已验收范围。

## 2026-09-19 智能地球的装饰与陆地材质清理

用户指出地球陆地块状感明显，下方九个菱形没有可理解的意义。本轮仅收敛智能场景的地球及其共用装饰，不修改供应链地图。

- 移除 `persistent-knowledge` 装饰对象及每帧矩阵更新；不再用小球或其他几何阵列替换。六阶段共享粒子及回流逻辑保留。
- 移除地球轨道上六件过小、难以辨认的产品卫星模型，保留轨道光点的运动。
- 地球保留既有高清表面和法线贴图，去掉内部国界描线，仅轻描海岸轮廓；降低金属度、清漆与环境高光，压低陆地亮度，减轻拼块与塑料反光感。
- 现有产品图片、总览构图、阶段入口及业务示例保持原功能。

验证：根目录 `npm test` 通过（主应用 131 项、嵌入模块 4 项、内容检查）；根目录 `npm run build` 通过，保留既有大分包警告。原连续形变测试改为验证共享粒子本身，删除对已撤掉装饰对象的要求，48 秒循环连续性检查保留。

浏览器完整加载后检查了总览及 1920×1080 地球详情，未见下方菱形或轨道小块，表面过亮光斑已降低；详情进入案例、返回同一阶段、回到总览可用，独立检查页控制台无错误。截图：`docs/qa/intelligence-flow/clean-globe-detail-1920.jpg`、`docs/qa/intelligence-flow/clean-globe-overview-1920.jpg`。未在实体触摸大屏验收，审美效果仍以用户观看动态预览为准。

## 2026-09-19 公司晶体定版与操作按钮统一

- 按用户最新要求，公司首页固定晶体模型，移除星光切换入口及生产场景中的星光实例、更新逻辑。旧 `?logo=starlight` 链接经浏览器完整加载仍显示晶体；历史星光源文件保留，当前首页不导入。晶体材质、反射与互动光效保持原效果。
- 以用户截图中“查看典型案例”为样式依据，新建 `src/components/exhibit-action.css`，统一细蓝描边、半透明深蓝底、银蓝文字、胶囊圆角与箭头，以及悬停、按下、键盘焦点和减少动态状态。公司、品牌、品类、供应链详情、智能总览/详情/案例及资料阅读器的主要操作和返回入口接入共用样式；导航、筛选和场景点击区域保持各自语义。
- 浏览器实际检查：公司故事打开与返回；品牌详情操作；品类的探索与进入场景；中国研发/核心供应链入口及返回地图；智能总览到阶段详情、查看示例和返回同阶段。原点击逻辑保留，检查页控制台错误为零。
- 1920×1080 检查公司与品牌；1366×768 中文及 1200×900 英文检查品类按钮，两项操作无相互遮挡且不压住底部导航；1200×900 英文检查供应链与智能页面。3840×2160 仅检查智能详情按钮的计算布局，字号随视口放大、边界处于视口内，不宣称本轮完成完整 4K 视觉或实体触摸屏验收。
- 根目录 `npm test` 通过（主应用 131 项、嵌入模块 4 项及内容检查）；根目录 `npm run build` 通过。最终清理旧样式选择器后主应用构建再次通过；保留既有大分包体积警告。未发布生产环境。

## 2026-09-19 数据资产改为外部信息与专家知识汇聚

用户明确的数据资产范围为互联网渠道、行业报告、发展趋势，以及公司行业专家和业务专家的知识。本轮据此替换第一阶段原来的三张产品档案，保留总览和其他阶段允许使用的品类示意图。

- `intelligenceAssets.js` 创建真实三维报告页、趋势信号、专家节点与中心知识结构。银蓝表示外部信息，暖金表示专家知识，五条稀疏光流携带信息汇入中心；不加入未核实的数值、来源品牌或实时接入状态。
- 去掉第一阶段原来的内部系统标签，固定展示五类来源；总览、资产详情、分析说明和资产案例同步改写。案例明确多渠道采集、保留原文来源、人工智能辅助整理，以及专家校核和待验证判断。
- 第一阶段案例沿用来源与知识模型，不跳到产品成果场景。资产到分析的共享粒子目标改为中心知识结构，保留现有渐变显隐与粒子位置插值；不宣称主网格已经实现任意拓扑形变。
- 沿用父级唯一动画时钟和资源释放。无新增依赖或生成图像；可拖动观察，静置有流动，减少动态偏好下冻结运动。

验证：根目录 `npm test` 通过（主应用 132 项、嵌入模块 4 项、内容检查），根目录 `npm run build` 通过，保留既有大分包警告。新增行为检查覆盖五类来源、无产品图片、信息流持续运动、减少动态冻结及资产案例隔离。

浏览器已操作资产入口、案例、返回同阶段、切到分析、切换语言；1920×1080、1366×768 中文与 1200×900 英文的模型、文案和固定导航无遮挡，检查页控制台无错误。截图：`docs/qa/intelligence-flow/knowledge-assets-1920.png`、`knowledge-assets-case-1920.png`、`knowledge-assets-1366.png`、`knowledge-assets-narrow-en.png`。未进行实体大屏验收或生产发布；页面继续明确标注场景示意、未接入实时数据。

## 2026-09-19 数据资产模型内部动态增强

用户指出连接线上的流动不足以表现模型自身的生命力。本轮保持数据资产的模型构图、来源含义和界面位置，增加错开节奏的局部动作：互联网节点传递信号；报告逐行扫描、页层轻微展开；趋势曲线连续变形并带有游标；专家节点出现环形阅读光与扩散反馈；中心关联网络持续呼吸、变形和传递信号，光学外圈与索引层独立运动。入口及案例共用同一模型工厂。

- 曲线在原有顶点和法线缓冲区中变形，没有逐帧新建几何体；时间全部来自父级共享时钟，无新增循环、依赖或图片素材。
- 新增测试逐一检查局部对象在 1.8 秒内的几何或变换变化、重复变形后的合法顶点与法线、缓冲区复用、暂停与减少动态偏好。根目录 `npm test` 通过：主应用 133 项、嵌入模块 4 项及内容检查；根目录 `npm run build` 通过，仍有既有的大分包警告。
- 浏览器在 1920×1080 连续采样 18 帧，确认停留在数据资产时模型内部持续变化；保留首尾画面 `docs/qa/intelligence-flow/asset-local-motion-a.png` 与 `asset-local-motion-b.png`。暂停后再采样，后段截图字节完全一致；恢复后继续运动。检查页控制台无错误。
- 本轮仅增强数据资产及其案例模型，不宣称其他五阶段已经重做局部动作，也不宣称完成实体大屏或生产验收。


## 2026-09-19 星空背景统一与独立星点运动

本轮以用户最新反馈为准：撤掉整张背景图的旋转、扭曲和迁移。保留原蓝色星云构图，星云固定，星星单独运动。旧旋转方案的截图不作为最终验收证据。

- 共享 `spaceAtmosphere.js` 统一主应用与嵌入模块的背景实现；智能场景不再叠加独立静态背景平面。公司保留现有晶体和动态湖面，天空与其他页面采用同一素材和星点系统。
- 图像只承担静止星云；2,600 个独立点通过已有图形渲染循环按各自的深度、速度和相位缓慢漂移与闪烁，无共同旋转中心。远处较慢，近处较快；在屏幕外循环衔接。
- 沿用共享时钟、质量参数、暂停和减少动态偏好；没有新增依赖或独立动画循环。嵌入模块共用实现已通过单元检查与重新导出构建，本轮不额外声称完成其全部交互验收。

### 验证范围

- 根目录 `npm test` 通过：主应用 136 项、嵌入模块 4 项及内容检查。根目录 `npm run build` 通过，仍有既有分包大于 500 kB 的提示；`git diff --check` 通过。
- 浏览器检查了 1920×1080 公司、供应链、智能总览，以及 1366×768 品牌与数据资产、1024×900 数据资产。3840×2160 检查数据资产画面与布局，画布和视口尺寸一致、无横向溢出。
- 品牌暂停后连续截图的后段字节一致；恢复后截图发生变化。智能总览再次连续采样 40 帧，保留第 5 帧和第 40 帧作为运动证据，控制台错误为零。星云采样函数无时间参数，星点速度各自独立的代码约束已加入专项检查。
- 没有实体大屏或触摸设备验收，没有生产发布；视觉运动是否达到用户期望仍以用户观看动态预览为准。

最终截图：
- `docs/qa/unified-sky/independent-stars-home-1920.png`
- `docs/qa/unified-sky/independent-stars-motion-a.png`
- `docs/qa/unified-sky/independent-stars-motion-b.png`

### 图像处理记录

使用内置 ImageGen 工具编辑，输入为既有 `public/assets/intelligence-v2/backdrop.png`，仅清理固化在图中的星点，保留蓝色星云。原图保留。

最终素材：`public/assets/intelligence-v2/nebula-starless-v1.png`。

<details>
<summary>最终图像编辑提示词</summary>

Use case: precise-object-edit. Asset type: starless nebula clean plate for a realtime 3D web exhibition background. Edit the attached original background: remove EVERY individual star, bright point, sparkle, speck, particle, and diffraction spike. Preserve ONLY the existing very dark black/navy negative space and blue nebulous gas filaments, maintaining the EXACT original wide 16:9 composition, position, exposure and blue palette: luminous thin blue nebula concentrated in the upper left along the rising diagonal; much fainter gas across upper right and right edge; center and lower half almost black. Keep the gas filament detail and natural cloud texture; do not blur the entire image, do not add new clouds or objects, do not brighten the overall background. All removed points must be filled seamlessly with neighboring smooth dark sky or continuous gas, absolutely no stars anywhere. This is the stationary background layer; individual stars will be rendered as real moving particles separately in code. No text, no logos, no UI, no planets, no rings, no new motifs. Deliver a clean 16:9 landscape background.

</details>


## 2026-09-19 供应链入口胶囊化与共用按钮流光

按用户要求，美国、中国、柬埔寨、越南的地图入口，以及地图下方区域入口接入既有 `exhibit-action` 样式。保留国家名、业务说明、地理锚点和关联线；中国的核心枢纽说明并入名称行。入口统一细蓝描边、深色半透明底、圆角和右箭头，未改变国家的场景/数据分布行为。

共用按钮增加 8 秒一轮、带停留间隔的银蓝流光，边缘较亮、表面极淡，国家入口错开相位。伪元素不接受指针事件；保留键盘焦点。全局暂停、阅读覆盖时的底层场景暂停、页面隐藏以及减少动态偏好处理均接入现有状态，没有新增动画计时器、依赖或图片。

验证：
- 根目录 `npm test` 通过：主应用 136 项、嵌入模块 4 项及内容检查。根目录 `npm run build` 通过；最后一次窄屏样式修正后再次构建通过。保留既有大分包警告。`git diff --check` 通过。
- 1920×1080 地图、1366×768 中文及 1024×900 英文实际截图检查。窄屏底部区域按钮首轮发生内部挤压，已收紧字距与留白；复查四按钮 scrollWidth 等于 clientWidth，点击高度均 44 像素。3840×2160 英文检查入口均处于视口内且内容无横向溢出。
- 实际点击越南显示客户 2 / 供应商 65 的既有主档数据；美国进入三维协作场景、返回地图、恢复全景均正常。首页故事打开/返回、智能总览进入分析详情已操作。
- 浏览器采样流光伪元素位置，18 次采样有 9 个不同位置；暂停后四个国家入口均为 paused 状态。检查页控制台错误为零。
- 最终地图截图：`docs/qa/unified-actions/supply-capsules-1920.png`。未进行实体触摸大屏验收或生产发布。


## 2026-09-19 总览与价值实现三个可切换候选已执行

按用户明确的增量制作要求，同时实现 A 产品成形、B 全球洞察、C 客户选品，在智能总览右上角比较；所选方案沿用到价值实现详情及案例。原模型工厂和原素材保留，当前总览不再构建三张产品卫星图片，也不恢复已删除的重复六阶段栏。

- A 为原创便携创作套装三维概念：真实盒壁、铰链、内托、笔、颜料杯、纸层、提手和金属搭扣。盒盖开合、单笔取放及两种配色可操作。修正盖内印刷图形的相交和装饰压印高度。
- B 保留地理轮廓与大气层，去掉随机城市灯光和装饰金色轨道；研究区域可切换，随后地球退到背景、焦点移向同一真实体积产品。区域只是组织研究的入口，不虚构市场机会。
- C 比较随行与共享桌面两种组合；独立笔筒和画本参与选入、退出与陈列。三个候选均以待验证问题收尾，反馈光点归入评审记录；没有虚构成交或效果指标。
- 每套共用 24 秒三步演示，可手选动作或恢复自动演示。模型更新使用父级唯一时钟，沿用暂停、页面挂起和减少动态偏好；切换及案例复用已分配几何，没有新增依赖。

验证：
- 根目录 `npm test` 全部通过：主应用 137 项、嵌入模块 4 项及内容检查。新增检查覆盖三方案变化、方案与区域保持、组合取舍、减少动态冻结、资源复用及循环边界。
- 根目录 `npm run build` 通过，仍有既有的分包超过 500 kB 提示；`git diff --check` 通过。
- 浏览器实际操作 A/B/C、研究区域、桌面组合、阶段详情、业务案例及返回；B 的亚太选择在往返后保留。暂停时共享时钟保持 1.72 秒，恢复后继续更新。检查页控制台没有错误。
- 1920×1080、1366×768 中文与 1200×900 英文已做截图和控件边界检查；3840×2160 的画布/视口布局检查无横向溢出、无按钮越界，演示控件底边 1792、导航顶边 1813。未进行实体大屏和真实触摸设备验收。
- 截图位于 `docs/qa/intelligence-proposals/`。当前为可比较的原创概念样机，不宣称复刻某一在售产品或接入真实业务记录。没有生产发布。
# 2026-09-19 · 第一版跨电脑交接检查点

- 用户要求先完整上传当前项目，另一台 Mac 继续第二版；当前视觉未通过用户验收。
- 本轮写入四个实时几何场景：玻璃知识书页、切面洞察晶体、便携/共享产品比较、体积无限循环装置。AI 驱动分析和价值实现的候选场景保留。
- 决策选择可显式带入价值实现的客户选品方案；详情中的方案比较入口仅在价值实现阶段显示。洞察、决策和进化案例保留自身主体。
- 根目录测试通过，主应用 137 项、产品模块 4 项；内容校验通过，完整构建通过。构建仍有现有的大分块提示。
- 本机页面已查看新场景；本轮检查时未捕获到浏览器错误。已取消临时视口设置。尚未进行真实大屏、触摸设备和长期运行验收。
- 这些记录是实现和本地检查状态，不代表精致度、业务表达及最终视觉已获认可。
