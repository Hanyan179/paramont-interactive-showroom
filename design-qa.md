# 智能与洞察：模型与连续形变复核

日期：2026-09-20；分支：`design-v2`。

用户认为上一版过于简陋。上一版的流程通过不能代表视觉品质通过，本轮重新制作模型、材质、图表、商品页和中间帧，保留左侧叙述与底部六阶段布局。

## 依据与范围

依据用户六阶段说明及六宫格参考图。用户文字优先：美妆眼影盘替代香水；右侧晶体向左投射；卡片形成轻透视环而非复杂圆柱；克制蓝白光效；108 秒连续循环。参考图是美术方向，不是逐像素复刻目标。

本地入口：http://127.0.0.1:5183/?quality=studio#/atlas/home ，选择“智能与洞察”。开发环境加 `motionReview=1` 可精确停帧；生产构建不显示审阅控件。

## 本轮改动与检查

- 数据资产：98 个玻璃单元、50 个商品/图表/评价缩略面，形成有前后层次的立方体；文字沿曲线收束进入深度空间。
- 分析与晶体：扫描依次重排内部单元，关系节点随后浮现；立方体与菱形共用对应顶点，内部单元向核心收束；新增切面反射、透明外壳、光学核心及接触光。
- 洞察：分析屏在晶体左侧形成，趋势、情绪、预测和机会逐项绘制。使用独立图表纹理，避免上一版只有栏目名称。
- 决策：同一批分析卡片放大、展开、绕行并高亮推荐。中间帧发现商品经过错误卡片，已调整为 55 秒前归入推荐卡，再随该卡运动。自动测试覆盖 55、58、61、66、69 秒的位置对应。
- 价值：同一个眼影盘纹理从推荐卡放大为商品主体，再缩入暖白商品详情页；补充色板、价格、评分、购买示意和评价。缩短、压低倒影，分离商品说明文字。
- 反馈：评价从商品页释放为有尺寸层级的卡片；明确排布避免相互遮挡，再退去卡框、收束成最初的一句话。
- 暂停交互：暂停时阶段文案立即显示；修复“继续演进”只恢复时间轴模式、没有恢复播放的问题。

## 视觉证据

本地截图位于 `展示应用/qa/intelligence-detail/`，按项目既有规则忽略，不提交版本管理。

- `reference-and-implementation.jpg`：用户参考图和六个实际阶段在同一输入中并置检查。
- `before-after.jpg`：旧版与本轮的数据立方体、洞察屏、评价回流局部对比。
- `final-16.png`、`final-33.png`、`final-47.png`、`final-67.png`、`final-87.png`、`final-98.png`：实际浏览器阶段截图。
- `final-58.png`：商品已归入推荐卡片的中间帧。
- `final-77.png`：商品主体、倒影与说明文字。
- `analysis-1920.png`：1920 × 1080 的图表、晶体及完整布局。
- `commerce-1366.png`：1366 × 768 的商品页及完整布局。
- `start-1920.png`、`final-107.9.png`：开场与结束时均仅保留同一条评价，构图相同。

快速连续截屏时曾出现浏览器合成画面滞后，不能将批量截图时间标签作为精确帧证据。精确零点的第一次截图出现空画面，后续独立截图确认零点评价正常；未据此虚构代码修复。主要证据采用分开操作的独立截图，循环对象一致性另由测试验证。

字体、留白、颜色、图像、文案五项均重新检查。图表细字与立方体微缩内容属于模型细节，主要叙述在左侧；商品为生成式概念素材，价格、趋势、订单及评价均为演示。未使用真实电商品牌标志。

## 工程验证与边界

- `npm test`：商品模块 4 项、主应用 135 项通过；内容关系与本地素材检查通过。
- `npm run build`：通过，仍有既有的大代码块体积提示。
- `git diff --check`：通过。
- 自动验证包括三轮时间采样、边界连续性、首尾可见对象一致、资源复用与释放、暂停与跳选、案例保护及多尺寸安全区。
- 本轮实际截图覆盖 1280 × 720、1366 × 768、1920 × 1080。3840 × 2160 有布局安全区测试，不宣称完成实体 4K 大屏验收。
- 没有新增运行依赖；没有推送远端或发布生产环境。

## 评审结论

本轮已完成具体的视觉细化与中间帧修正，可以进行用户复核。不能将工程测试通过等同于用户对美术品质的认可。实时晶体的折射复杂度和参考图的离线渲染仍有差距；目标触摸大屏的观看距离、实际帧率与 30 分钟稳定运行尚需现场验证。

final result: passed（本轮本地实现及视觉复核范围；非生产发布或用户最终验收）

## 跨电脑同步前检查

推送前当前工作目录包含新增的眼部、投射及文字工具和对应 MIT 许可。回归检查发现眼部对象未受时间轴控制，导致首尾可见对象不一致；已将眼部显隐绑定核心的生命周期，并连接投射几何与屏幕位置。尚未启用的输入光标和扫描线保持隐藏。重新通过 139 项测试及完整构建。本次同步不代表新增细节已完成浏览器美术复核。

## 追加迭代：文字生成与机器人意图（同日）

用户进一步要求“丝滑与灵动”，本轮将验收重点从模型细节移到行为与空间关系。

- 字母：G 开始，逐字生成完整评价；其他评价错峰输入，卡框随后出现；商品缩略面延后到汇聚前进入。结尾完整评价逐字收回 G，首尾使用同一纹理、位置与裁切状态。
- 角色：眼部增加深色球面底座、虹膜、瞳孔、高光；短促闭眼与较缓睁眼；眼睛先偏向屏幕，身体延迟跟随，悬浮与转动幅度分离。
- 空间：分析屏具有厚度，先朝向机器人倾斜；卡片和商品共享屏幕朝向。屏幕随推进转向观众，然后接续卡片展开。
- 扫视：取消原有贴片投射，四面体积光束源点绑定眼睛，远端绑定斜屏上的扫描带，随眼睛与屏幕位置实时更新。
- 开源复用：适配 pmndrs/drei 的 Float 与 SpotLightMaterial。完整 MIT 声明随发布素材保存在 `public/licenses/drei-MIT.txt`，来源和适配范围记录于 `src/impact/vendor/README.md`。调研过 three-mesh-ui、uikit、troika，未为本次固定内容重复引入界面布局和字体运行时。
- 本轮发现并修正：倾斜屏幕透明壳体遮住图表；缩略图过早出现干扰阅读；眨眼时核心仍亮；关系节点透明后仍写深度。
- 新增测试：逐字生成与反向收尾、机器人眨眼和朝向、体积扫描起点与深度。主应用 138 项加商品模块 4 项通过，构建通过。浏览器未发现着色器错误。

证据目录：`展示应用/qa/intelligence-presence/`。字母、评价卡片、眨眼及空间分析屏采用独立浏览器截图检查。本轮无实体大屏帧率验收；对“灵动”的最终审美认可仍由用户观看动态预览判断。

## 追加迭代：密度取舍与选定机器人本体

用户选择第二个机器人方向，并要求圆润、精密、沉稳的决策者气质。本轮先生成该方向的收敛概念，再实现可实时旋转的曲面外壳、石墨色接缝、曲面黑玻璃面罩、面罩包边和独立眼部。概念图未充当场景模型或贴图。

- 数据结构从 42 条重复信息、50 张重复缩略面、98 个小格减至 6 条唯一评价、零重复缩略面、8 个大玻璃体块。
- 同一张评价卡沿错峰曲线进入指定立方体表面，并继承立方体朝向；没有额外创建一套同内容的图片流。
- 立方体在形成后持续缓慢旋转，大体块呼吸、扫描时错层复位。手动阶段停留的微动使用已有共享运动时钟，暂停时保持不变，没有另起动画循环。
- 机器人抛弃透明宝石主体，使用圆润银色前后外壳和黑玻璃曲面面罩；降低眼睛亮环厚度，增加细虹膜纹理与小高光。外壳与脸部为真实网格，保留眨眼、朝向和空间投射。
- 146 项测试及构建通过。新增唯一信息数量、同卡归位、大体块重排、圆润外壳及曲面面罩、手动微动与暂停冻结的验证。
- 浏览器截图位于 `qa/intelligence-refinement/`：`gather-11.png`、`cube-16.png`、`robot-final.png`、`robot-screen.png`。`robot-concept-comparison.jpg` 将选定概念与实际渲染放在同一输入中比较。

可见取舍：实时版减少了概念图的复杂后壳分件和光学反射层数；当前面罩与外壳曲率是可控程序曲面，不宣称与离线概念图逐像素一致。主要轮廓、银色与黑玻璃材质分区、独立眼部和无四肢结构已落地。最终精英感与观看节奏需要用户看动态画面复核，实体大屏性能仍未现场验收。

## 当前主线迭代：语言、扫视与评价回流

按用户认可的整体流程继续打磨，保留页面布局和选定机器人，不重新设计整套场景。

- 中文与英文展示分开：移除阶段标题的第二语言；同一批画布在切换语言时原位重绘，模型内栏目、设计信息、购买评价、商品说明及收尾文字均随语言切换。保留对象与纹理身份，不重启时间轴。
- 六条信息分别归入六个面，场景标签区分用户声音、商品设计、市场信号、色彩趋势、使用场景、品质反馈；加大旋转幅度。面片仅显示正面，避免透明立方体中出现背面镜像文字。
- 机器人眼睛左右扫视，身体略有延迟地跟随；投射带绑定视线横向扫描。已有眨眼与悬浮保留。
- 分析和决策阶段撤去商品 PNG，改用色彩、形态、触感和场景的文字设计板，推荐卡汇总设计方向；进入价值实现时才显现商品。分析卡增加实体厚度，预测柱形改为独立几何对象，逐根生长；修正后排柱形越过前排卡片的遮挡。
- 商品页先呈现商品，随后在圆角视口内下滑；商品与说明受页面边界裁切。第一条购买评价在下滑后出现，其他评价逐项增加；同一条文字配合购买样式与展厅样式渐变，再离开页面、聚合、退回文字。首尾沿用上一版逐字循环。
- 自动播放速度提高 25%，108 秒逻辑时间轴对应约 86.4 秒实际播放；空闲等待仍按实际秒数，暂停与案例阅读保持冻结。

本轮实际浏览器截图位于 `展示应用/qa/intelligence-iteration/`：中文/英文分析屏、纯文字决策环、商品页下滑前、首条购买评价、多条购买评价、评价转为展厅样式。浏览器审阅时使用逻辑秒数。测试增加语言原位重绘、商品阶段限制、下滑先于评价及 1.25 倍播放验证。当前改动未自动推送或发布；未作实体触摸大屏性能声明。

验证结果：150 项测试（商品模块 4、主应用 146）及根目录完整构建通过；保留既有代码块体积提示。中英文模型截图已核对；页面下滑时越界、立方体镜像背面文字、反馈卡片交叉与预测柱形遮挡均经实际截图检查后修正。未新增运行依赖。


## 2026-09-20 合并远端优化，保留本地高山机器人

按用户本轮选择，合并远端 `design-v2` 的 `aac2c78`，机器人采用本地 `0845166` 中的山形程序模型。本节取代上一节关于远端菱形机器人造型的采用结论。

- 保留远端六条唯一评价、八个数据体块、同卡归位、错峰汇聚、屏幕透明层次、先看后转身及手动停留微动。
- 本地机器人由银白山形外壳、深蓝面罩、两只光眼组成；造型模块原样保留，没有引入生成图片或远端菱形外壳。眼部扫描源跟随山形机器人的双眼位置。
- 数据体块收拢后进入山形机器人，后续分析屏、推荐商品与反馈循环保留。继续沿用父场景时钟及资源释放机制。
- 完整构建、内容检查与 146 项测试通过（主展厅 142 项、商品模块 4 项）。现有大包体积提示仍在，无新增依赖。
- 浏览器实际检查时间轴第 16、33、43 秒：远端数据汇聚、本地山形主体和屏幕投射均可见。合并期间的热更新冲突错误在解决并重载后恢复。本轮未进行实体大屏验收，也未推送远端。

## 2026-09-20 主线续作：新机器人表情、报告方体与购买评价

已拉取并合并 `origin/design-v2` 的 `5a192fd`；此前本地语言、速度和评价回流优化保存在 `c9550ef`，合并检查点为 `5ad4f3a`。以下为合并后继续实现的本轮改动。

- 保留更新的山形机器人，增加开心弧形双眼与轻跃、被否决的叉形双眼与摇动、选中的圆形双眼与确认勾和点头；保留扫视与眨眼。眼部符号作为面罩显示层绘制，修正左眼被遮挡。
- 数据资产增加 36 条双语信息，按六类汇入六份完整报告。报告含标题、图表及设计信息，随后归位为方体六个面；不再用六条评论充当方体贴图。报告及文字复用现有时间轴和资源释放机制。
- 商品页面先下滑，再出现购买评价。评价含原创绘制头像、姓名、已购买标记、星级、日期、具体正文与款式信息；随后逐步增加、变为展厅卡片、退回文本。所有内容为演示数据。
- 保留中英文分开显示和 1.25 倍播放速度。未新增依赖。

验证：根目录 `npm test` 通过（商品模块 4 项、主应用 148 项及内容检查），`npm run build` 和 `git diff --check` 通过；仍有既有大分块提示。浏览器截图核对信息密度、报告方体、三种眼睛表情及购买评价，证据在本地 `展示应用/qa/intelligence-mainline/`。本轮截图为 1280×720，未新增实体大屏、真实触摸及长时间运行验收。当前改动未推送、未发布。

## 2026-09-20 机器人贯穿审查、选择与商品页

按用户新确认的连续叙事调整：机器人从数据中诞生时开心；洞察阶段由五份文件成为前景，机器人在右侧逐份扫查，两个方向被标记为暂不采用，推荐方案被选中。扫描目标在文件间平滑移动。决策阶段沿用同一批卡片，聚焦并放大选中的方案；美妆商品显现后机器人保留在旁边，二者一起进入商品页，同一个山形机器人缩小到页头作为标志，商品进入原有商品图区域，商品信息不变。随后沿用下滑、购买评价及反馈回流。同步更新双语阶段说明。

验证：153 项测试（主应用 149、商品模块 4）及内容校验、根目录完整构建通过，仍有既有的大分块提示。测试覆盖文件主体及状态、扫描源与文件目标、机器人持续存在到页头、资源复用和循环边界。实际浏览器核对中文文件审查、英文选中、商品与机器人同框、商品页标志；修正商品放大遮住机器人的构图。截图位于本地 `展示应用/qa/intelligence-mainline/`。本轮按用户明确要求提交并推送当前 `design-v2` 分支；不进行网站发布。实体触摸大屏与长时运行仍待用户设备验收。

## 2026-09-20 凌晨迭代：研究内容、五类美妆与连续加工

在已认可的六阶段主线上继续迭代。保留左侧文案、底部阶段导航、中英文独立显示、1.25 倍播放速度，以及商品页下滑、购买评价增长和反馈回到文字的顺序。

- 将 36 条信息按演示评价、公开商品形态、质地参照、四色分工、结构要求和验证事项重新组织。六份报告写出具体观察与下一步问题，删除没有依据的增长曲线、预测柱图和占比图。公开资料依据见 `展示应用/docs/智能展厅内容依据.md`；不将演示评价当作真实消费者研究。密集文字保留数量，排布避开主要评价卡。
- 五份提案分别为唇油、腮红、轻透底妆、高光棒和四色眼影，各有独立内容构图。前四款使用程序几何制作瓶、刷头、软管、滴管、膏体与盖体，最后一款沿用现有眼影盘图像；不使用真实品牌标志。逐张审查时提案进入前景，其他提案退后；修正模型与标题、说明及状态文字的碰撞。
- 山形主体保持更新后的造型。两只眼睛用同一套连续曲面表现专注、怀疑、不耐烦、轻喜悦和选中，配合瞳孔、眨眼、轻抬、侧倾和点头。眼神跟随实际扫描接触点；眨眼避开表情高点。报告和关系线收拢后再点亮眼睛；提案入场避开面罩。
- 选中的眼影提案进入加工：四色用途分离、逐格归位、镜面与盒体组合、商品显现。现有商品图像被投影到六个互不重叠的表面，组装完成后与同一商品图像严格对齐；没有再切入另一张不对应的商品图。扫描目标随部件移动，步骤字幕错峰出现。
- 商品和同一个机器人一起进入商品页，机器人继续收拢为页头标志。评价回流时，新旧样式共享连续变化的外轮廓，文字颜色平滑变化，消除大小不同的双层边框与颜色跳变。
- 移除已不显示的旧分析屏、设计板、重复内容贴图和旧标签；同样的状态文案共用纹理。保留单一时钟与统一资源释放，没有新增依赖。

验证：根目录 `npm test` 通过，商品模块 4 项、主应用 157 项，共 161 项，另含内容检查；根目录 `npm run build`、`git diff --check` 通过。构建仍有原有的大分块提示。新增检查覆盖信息碰撞、产品与文案避让、入场面罩避让、眼神跟随、表情时序、逐件加工、评价轮廓一致、颜色连续与共享资源释放。三轮时间轴采样复用同一批物体、几何与材质。

浏览器逐段检查 6、17、29、35.9、39.1、41.6、44.5、46.6、49.2、57、65、74.5、85、90、95、96.5 等关键时间点，并核对中文、英文、案例打开与返回保留时间。1366×768、1920×1080、1200×900 已做可见画面检查；3840×2160 已做页面边界与三维投影检查，但当前浏览器的大尺寸截图存在裁切／拼接异常，不将该截图作为完整视觉验收证据。截图保留在本地忽略目录 `展示应用/qa/intelligence-craft/`。本轮没有实体触摸大屏或连续 30 分钟实机验收，不进行网站发布。

## 2026-09-20 凌晨续作：中央单字开合与只用双眼表演

按用户新增反馈，开场先保留屏幕正中央的单个汉字或字母，隐藏页头标志、阶段文案、返回入口、阶段导航及系统工具。文字增多后，整个信息场缓慢移向右侧，外围界面依次显现。反馈收尾反向执行：先收起界面，再将评价带回中央、逐字回退，回到与首帧相同的字形、位置和大小。界面显隐和三维构图共用既有时钟，暂停、手动跳转、案例阅读都不另起计时器；完全隐藏的控件同时退出点击与键盘导航。中段布局、双语独立显示、108 秒逻辑周期、1.25 倍速度及商品页反馈流程保留。

- 去除山形面罩下方容易被看成嘴巴的银色山脊，保留山形外壳；开心、审查、暂不采用、选中仍由同一对连续变形的眼睛及轻微身体动作表现。
- 修复评论、碎片文字和报告正文在汇聚时叠字的问题。评论卡框先收起，来源文字汇入报告；报告轮廓先出现，标题到正文依次展开。六份报告保留清晰阅读停留，再折叠成正方体，手动“数据资产”仍停在完成的六面立方体上。修复折叠时前后页面互相透字的深度遮挡。
- 修复商品模型在提案淡入淡出中被自身卡片背景覆盖、接近完全显示时突然变亮的问题，统一纸面、厚度、商品和印刷文字的绘制顺序。
- 四个色盘分别围绕自身中心运动，补充薄边并按扫描节奏依次落位；镜盖以铰链为转动中心。加工完成时各部件顶点与原商品图精确重合，保持连续交接。
- 英文标题使用单独的字号与行距，修复 1920×1080 下长标题挤压案例及返回按钮的问题。

验证：根目录 `npm test` 通过，主应用 163 项、商品模块 4 项，共 167 项，另含内容校验；`npm run build` 与 `git diff --check` 通过。新增检查涵盖中央单字首尾一致、界面显隐曲线、无嘴部凹口、报告阅读空隙、完整六面停留、模型绘制顺序及部件与商品图精确对齐。现有大分块构建提示仍在，无新增依赖。

浏览器核对了报告展开与折叠、提案转换、色盘落位、选中双眼、中文首末帧和英文中央字母；在正常页面运行了一个完整自动循环并越过接缝，未捕获警告或错误。中英文各六阶段在 1366×768、1200×900、1920×1080、3840×2160 共 48 组实际渲染后的页面边界与按钮间距检查通过。新截图覆盖 1366×768 与 1920×1080，保存在本地忽略目录 `展示应用/qa/intelligence-craft/`；4K 截图工具限制和实机验收边界沿用上一节说明。本轮提交推送 `design-v2`，不进行网站发布。

## 2026-09-20 继续逐帧修正：有序汇聚与机器人避让

继续检查过渡中间帧，发现静止画面正确时仍有两类遮挡：来源文字在收拢途中互相穿插；机器人直接飞向页头会穿过眼影盘，身体被商品遮住而双眼仍露出。保持既有布局、速度与目标位置，调整两段路径。

- 36 条来源信息按六份报告分组，每组以两列、三行的对应顺序收拢；出场仍交错进行，汇聚过程中保留阅读间距，不再让不同报告的信息交叉穿行。
- 商品成形时，机器人轻微退开留出边缘空隙；进入商品页时先上升，再沿上方弧线飞向页头，缩小为同一个标志。商品与机器人均保持原有实体，没有通过隐藏机器人掩盖遮挡。

验证：根目录 `npm test` 通过，主应用 165 项、商品模块 4 项，共 169 项，另含内容校验；`npm run build` 与 `git diff --check` 通过。新增回归覆盖整个文字收拢区间，以及四种规定屏幕比例下机器人与商品的投影边界、完整身体与顶部安全区。构建仍有既有大分块提示。

浏览器检查中文 1920×1080 信息汇聚与 81.5 秒机器人绕行、英文 1920×1080 报告生成及 1200×900 商品页落位；正常页面另运行完整自动循环，观察到时间自然跨越接缝，未捕获警告或错误。证据保存在本地忽略目录 `展示应用/qa/intelligence-craft/`。没有新增依赖，不进行网站发布；实体触摸大屏仍需接屏验收。

## 2026-09-20 形态交接续作：立方体重构与提案取出

逐帧检查发现，原先机器人出生时会叠在未消散的立方体上，内部方块透在面罩中；原来的旋转插值还会先停下，再反向转过大半圈。本轮让立方体原有顶点按现有山形外壳的真实表面重构，顺着原方向收正。内部数据先聚合并隐去，再让玻璃山形与银色外壳交接，随后双眼醒来。机器人最终几何、眼睛表演、后续位置和页头落位保持原有结果。

- 山形目标在场景创建时一次性采样，播放时仅更新既有顶点形变与实例矩阵；移除本动画中旧的菱形目标和重复晶体切面层。保留单层克制的玻璃反光，避免过渡中密集的三角亮斑。
- 眼影盘在提案内继承卡片的角度，取出后再逐渐转向正面；色彩用途标签与扫描目标跟随同一组部件坐标。末段仍与原商品图精确对齐。
- 复核加工起点发现镜盖与色盘放大后压住提案标题。调整为纸面与其他提案先收起，商品部件保持不透明再展开，去除加工过程中的标题和背景卡片叠压。

验证：根目录 `npm test` 通过，主应用 168 项、商品模块 4 项，共 172 项，另含内容校验；`npm run build`、`git diff --check` 通过。新增检查覆盖立方体与山形外壳的轮廓对应、材质交接、顺向旋转与角速度、提案角度继承，以及纸面退场时商品保持可见。原有三循环资源复用、首尾接缝和四种屏幕比例检查继续通过；构建仍有既有大分块提示。

浏览器复看 28.5、29.8、30.3、51.5、53.5 等中间帧，在最终实现上确认中文山形重构、英文提案取出与加工；正常页面另完成自动循环并跨越接缝，未捕获警告或错误。关键证据为本地忽略目录中的 `mountain-reconstruction-28-zh-1920.jpg`、`mountain-material-handoff-zh-1920.jpg`、`proposal-extraction-en-1366.jpg` 与 `proposal-extraction-en-1920.jpg`。整体 108 秒逻辑周期、1.25 倍速度及商品页反馈顺序保持不变。本轮不进行网站发布，实机触摸大屏验收范围不变。

## 2026-09-20 审查理由与选择依据

五个提案的外观已有差异，本轮补齐审查过程中的简短理由。围绕「用途清楚、便于随行」这一本轮设计任务，随当前提案依次展示保留方向、待验证问题及眼影盘的四色组合价值。文案只表达原创概念评审，不暗示参照品牌已经存在实测问题；公开资料与概念判断的边界同步写入内容依据。洞察阶段和案例文案与这些判断保持一致。

说明采用同一时间轴，前一条先退出，下一条再出现；选中理由随后退场，将画面交给加工步骤。没有改变机器人表情、商品流程、播放速度和中段布局。

验证：根目录 `npm test` 通过，主应用 170 项、商品模块 4 项，共 174 项，另含内容校验；根目录 `npm run build`、`git diff --check` 通过，保留既有大分块提示。新增检查覆盖单条审查说明与当前提案对应、说明之间不重叠、加工前退场，以及四种规定屏幕比例下避让导航和左侧文案。

浏览器核对中文 1920×1080 的腮红审查、英文 1920×1080 的眼影选中、英文 1200×900 的底妆审查与加工交接。英文案例打开、阅读和返回后时间仍为 44.3 秒；恢复自动播放能继续进入商品成形，未捕获警告或错误。证据保存在本地忽略目录 `展示应用/qa/intelligence-craft/` 的 `proposal-review-*` 文件。没有新增依赖，本轮提交推送现有分支，不进行网站发布。

## 2026-09-20 单字开场后的暂停入口回归

在浏览器复现了新的操作边界：先暂停主展厅，再进入智能与洞察，时钟会停在零秒；由于新开场隐藏了外围界面，触摸用户无法点击继续或返回。本轮让暂停入口停在第一阶段完整的立方体画面，同时保留全局暂停状态。继续按钮恢复现有自动时间轴，正常播放入口仍从零秒中央单字开始，减少动态效果的静止模式维持原行为。

验证：根目录 `npm test` 通过，主应用 171 项、商品模块 4 项，共 175 项，另含内容校验；`npm run build` 和 `git diff --check` 通过，仍有既有大分块提示。新增回归覆盖暂停入场的完整界面、保持时间、取消旧跳转、恢复立即前进，以及正常重入仍从零秒开始。

在无审阅工具的正常浏览器入口实际操作：主展厅暂停后进入，时间停在 16 秒且继续与返回按钮可见；点击继续后进入 30.375 秒；返回并重新进入时观察到 0.269 秒的中央开场，外围界面隐藏。未捕获警告或错误。中文 1920×1080 证据保存在本地忽略目录 `展示应用/qa/intelligence-craft/paused-entry-zh-1920.jpg`。本轮提交推送现有分支，不发布网站。

## 2026-09-20 扫描过程中的关联显露

复看智能驱动分析的中间画面，发现报告纸面遮住内部关系，原有线段的另一端也没有连接到任何节点。保留原立方体、扫描方向和形变时间，改为扫描经过的位置逐渐变成透明表面，依次显露内部六个节点。七条克制的连接线沿真实节点生成，随后与节点一起收拢，不再出现悬空端点。报告表面与扫描片共用立方体坐标，旋转中仍保持切面对应。

在关联显露时降低内部玻璃块及扫描面的亮度，让关系结构成为这一小段的阅读重点。全部使用原有时钟和固定数量的几何、材质；没有增加粒子或界面装饰，正常数据资产画面及后续机器人形态保持不变。

验证：根目录 `npm test` 通过，主应用 173 项、商品模块 4 项，共 177 项，另含内容校验；`npm run build` 与 `git diff --check` 通过。新增回归覆盖节点自上而下显露、七条线段的端点在完整和收拢状态均连接节点、旋转后的六份报告共用扫描平面，以及收拢结束后的退场。既有三循环资源复用和释放测试继续通过；构建保留原有大分块提示。

浏览器核对中文扫描中间帧、英文关联完成与山形收拢，在 1366×768 可见画面确认节点和扫描位置；恢复自动播放从 18 秒经过本段并继续到 50.521 秒，未捕获警告或错误。关键截图保存在本地忽略目录 `展示应用/qa/intelligence-craft/`：`analysis-scan-reveal-zh-1366.jpg`、`analysis-linked-structure-en-1366.jpg`、`analysis-gather-to-mountain-en-1366.jpg`。本轮提交推送现有分支，不发布网站，未扩大实体大屏验收结论。

## 2026-09-20 商品页滚动与评价样式交接

逐帧复看后半程，修正三处可见问题。机器人在成为页头标志后，与商品共用页面的上下裁切边界，随滚动从页内离场；之前的飞行弧线仍不裁切。首批评价卡继承页面滚动位移，在「购买者评价」标题下方出现，避免标题经过固定卡片时被遮挡。购买评价与展厅评价分别拆出卡片底色和外围信息，先退去头像、姓名与购买信息，再显现展厅标签；核心评价文字连续保留，外轮廓继续同步变化。

验证：根目录 `npm test` 通过，主应用 176 项、商品模块 4 项，共 180 项，另含内容校验；`npm run build`、`git diff --check` 通过。新增回归覆盖评价与滚动标题间距、四种屏幕比例下标志的完整飞行和页内裁切，以及样式转换时外围文字不重叠、主评价不断开。资源统一释放、三循环复用与首尾单字接缝继续通过，构建仍有既有大分块提示。

浏览器复看英文 1920×1080 下的 87.4 秒标志离场与 89.7 秒首评，中文 1200×900 下的评价列表和 96 秒样式中间帧，以及英文 97.5 秒展厅卡片。恢复 85 秒自动播放后再次观察到下一轮 3.896 秒，未捕获警告或错误。证据保存在本地忽略目录 `展示应用/qa/intelligence-craft/` 的 `commerce-logo-scroll-*`、`commerce-first-review-*`、`review-skin-*` 文件。

复看同时发现评价卡散开途中仍有局部交叉，将在下一轮继续调整路径；本轮不将整个视觉迭代标记完成。提交推送现有分支，不发布网站。

## 2026-09-20 评价散开轨迹避让

修正上一轮发现的评价卡交叉。两侧卡片先向外让出空间，中排左卡从主评价外侧绕行，中排右卡先穿过两列之间的空隙上升，主评价随后移向中央并放大。纵深变化留到平面位置基本稳定后开始，避免透视放大重新挤压间距；购买列表的起点和展厅卡片的终点均保持原布局。

验证：根目录 `npm test` 通过，主应用 177 项、商品模块 4 项，共 181 项，另含内容校验；`npm run build`、`git diff --check` 通过，仍有既有大分块提示。新增回归在四种规定屏幕比例下，以 0.025 秒步长检查 94–101 秒的六张卡片投影，卡片之间保持间隔，且避让左侧文案与导航。

浏览器核对英文 1920×1080 的 97.5 秒、中文 1200×900 的 98.2 秒，确认卡片分离、主评价可读；从 94 秒恢复自动播放后再次观察到下一轮 32.041 秒，未捕获警告或错误。关键证据保存在本地忽略目录 `展示应用/qa/intelligence-craft/feedback-path-clear-en-1920.jpg` 与 `feedback-path-clear-zh-1200.jpg`。本轮提交推送现有分支，不发布网站，不将浏览器验收等同于实体触摸大屏验收。

## 2026-09-20 六份报告的内容与视觉对应

复看发现六份报告虽内容不同，仍基本共用小字列表。保留六块既有报告对象、折叠路径与扫描方式，重新绘制内部内容：反馈引语、包装形式轮廓、质地对照色带、四色用途矩阵、盒盖与色盘结构示意，以及未勾选的样品验证清单。放大标题、主观点和用途标签，区分主信息与来源、边界说明。四色和结构示意与后续加工的眼影盘对应。

这些图形均为定性资料整理和原创设计说明，没有添加销量、比例、趋势预测或已通过验证的结果。中英文各自绘制，来源类型和待验证问题继续保留；内容依据已补充图形的表达边界。

验证：根目录 `npm test` 通过，主应用 177 项、商品模块 4 项，共 181 项，另含内容校验；`npm run build`、`git diff --check` 通过。没有为纯绘制调整添加复述实现的测试，既有折叠、扫描、资源释放、双语与循环检查继续通过，构建仍有既有大分块提示。

浏览器复核中文 1920×1080 的完整报告、英文 1366×768 的报告与 17 秒立方体可见面，未发现文字溢出或语言混排。从 8 秒恢复自动播放后观察到 37.456 秒，完成报告生成、折叠与扫描并继续进入提案，未捕获警告或错误。证据保存在本地忽略目录 `展示应用/qa/intelligence-craft/reports-distinct-zh-1920.jpg`、`reports-distinct-en-1366.jpg`、`report-cube-en-1366.jpg`。布局、速度、机器人与后半程保持不变，本轮提交推送现有分支，不发布网站。

## 2026-09-20 唇油提案的取用结构

审查五个提案时发现唇油的刷头藏在不透明瓶身内，说明文字中的取用方式无法从模型看出。将盖子、刷杆与刷头组织为瓶侧的同一组装件，保持连接关系和克制的倾角。首版复看发现盖子靠近右侧文案，已收紧横向间距；最终瓶体、刷头及说明文字分离，原卡片位置、聚焦节奏和审查动作保持不变。

验证：根目录 `npm test` 通过，主应用 177 项、商品模块 4 项，共 181 项，另含内容校验；`npm run build`、`git diff --check` 通过，仍有既有大分块提示。沿用现有商品模型范围、文字避让、淡入淡出和资源释放检查，未为纯摆位调整新增测试。

浏览器确认英文 1920×1080、中文 1200×900 的 39.3 秒提案：刷头露出，盖子未覆盖右侧说明或标题。另复看腮红审查、底妆待验证、眼影选中及四色加工，机器人仅使用眼睛与身体动作，未发现新的遮挡。未捕获警告或错误。证据保存在本地忽略目录 `展示应用/qa/intelligence-craft/lip-applicator-en-1920.jpg` 和 `lip-applicator-zh-1200.jpg`。本轮提交推送现有分支，不发布网站。

## 2026-09-20 加工与购买体验的文案分工

价值实现的案例步骤仍沿用样品测试，与已经进入商品页的画面不完全一致。本轮将决策赋能的动作与案例标题明确为提案组合成商品；价值实现改为同一件商品进入购买体验，步骤依次说明产品身份的延续、商品信息与购买入口、购买反馈与提案的关联。保留演示价格、评分和订单的说明，未增加实际销售或验证成功的暗示。

验证：根目录 `npm test` 通过，主应用 177 项、商品模块 4 项，共 181 项，另含内容校验；`npm run build`、`git diff --check` 通过，仍有既有大分块提示。仅修改双语内容，未改变布局、模型、时间轴或新增测试。

本轮从不带画质与审阅参数的正常入口复核。默认画质下，报告立方体、山形机器人与购买场景正常呈现；首轮各对象加载完成后，几何与纹理计数分别为 1722 与 144，之后继续播放 118.7 秒并跨越循环，计数没有增加，未捕获警告或错误。这里是有限时长的浏览器观察，不等同于长时间实体大屏运行。

中文 1920×1080、英文 1200×900 核对价值实现案例，英文窄比例另核对决策赋能案例，正文均完整可读。案例返回分别保留 85 秒与 67 秒；点击暂停后再次读取，剧情时间维持 67 秒、运动时间维持 482.77 秒。证据保存在本地忽略目录 `展示应用/qa/intelligence-craft/default-mountain-zh-1920.jpg`、`value-case-aligned-zh-1920.jpg`、`value-case-aligned-en-1200.jpg`、`craft-case-aligned-en-1200.jpg`。本轮确认的是满视口画面；当前内嵌浏览器未给出可确认的原生全屏状态，系统全屏与实体屏幕仍留待实机验收。本轮提交推送现有分支，不发布网站。

## 2026-09-20 选中提案时的识别与确认节奏

复看 47.9 秒发现：高光提案仍是扫描对象，机器人却已经睁大眼睛，提前进入原本属于眼影选中的识别表情。本轮让眼影提案先进入前景，再依次出现睁眼识别、一次确认点头和舒展的眼睛。确认动作从反复衰减摆动改为单次下点与回稳；选择明确后收住左右巡看，加工阶段仍保留呼吸式浮动和对色盘接触点的视线跟随。没有新增嘴部、表情图标或装饰特效，诞生、淘汰和不耐烦段落保持原节奏。

验证：根目录 `npm test` 通过，主应用 178 项、商品模块 4 项，共 182 项，另含内容校验；`npm run build`、`git diff --check` 通过，仍有既有大分块提示。新增回归检查上一份提案不提前触发识别、眼影进入前景后再识别与确认、点头在加工前结束，以及之后视线仍跟随部件。既有连续变化、四种屏幕比例、商品避让和机器人进入页头的检查继续通过。

浏览器核对英文 1920×1080 的 48.4 秒识别、中文 1366×768 的 49.1 秒选中确认；从 47 秒恢复自动播放后观察到 53.917 秒的四色提取画面，正常进入加工。关键证据保存在本地忽略目录 `展示应用/qa/intelligence-craft/recognition-timing-en-1920.jpg` 与 `selection-confirm-zh-1366.jpg`。本轮提交推送现有分支，不发布网站，实体大屏验收范围不变。

## 2026-09-20 恢复横线双眼与原有三角山脊

按用户新反馈，移除双眼中的瞳孔和高光小点，改回深色面罩上的两条白色圆角横线。开心与选中以线条弯曲表达，审查以宽度、高低和倾角变化表达，保留眨眼、视线跟随和确认点头。识别提案时只轻微伸展横线，不再睁成带瞳孔的眼形。

下方三角山脊恢复为最初山形模型的真实几何切口，露出银色主体；它是固定造型，不参与表情形变。本节取代此前将这部分作为嘴部视觉去除的决定。其他模型、布局、语言、速度和连续流程保持现有实现。

验证：根目录 `npm test` 通过，主应用 178 项、商品模块 4 项，共 182 项及内容检查；`npm run build`、`git diff --check` 通过，保留既有大分块提示。更新现有回归检查，确认只有两条无子装饰的眼睛、识别与点头时序不变，以及各表情下三角切口与银色山脊保持固定。浏览器在默认 1280×720 视口检查中文基础与审查状态、英文诞生与选中状态，无警告或错误；截图位于本地忽略目录 `展示应用/qa/intelligence-craft/robot-line-eyes-*`。本轮不发布网站，未扩大实体触摸大屏验收范围。
