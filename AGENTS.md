# 企业交互展厅仓库规则

- 本仓库归属数字样品房的前端看板展示，工作目录为该工作区下的 `前端看板/`；与同级业务前端 `pdmweb/`、业务后端 `pdmapi/` 分别维护，安装、构建和测试命令在本仓库根目录执行。
- 当前产品仅含四区主展厅与嵌入的品牌产品模块。具体开发规则见各目录 AGENTS.md，主架构文档在 展示应用/docs/开发与扩展指南.md。
- 共享代码、数据引用属于两套运行部分的共同输入；保持相对目录关系。改共享代码后先构建并导出产品模块，再构建主展厅。根目录 npm run build 已包含该顺序。
- 全局首页始终是 atlas/home 的 chapter 0。首页动作集中在 App.jsx 的 returnHome，关闭临时层并停止旧巡展恢复；产品浏览记忆由产品模块保留。不能退化为仅改网址。
- 三维热点和画布共用手势分类；拖动或取消后的兼容点击不能打开内容。键盘点击仍可激活热点。
- 沿用现有 5183 本地服务；GitHub 上传不等同网站部署，不擅自把私有仓库或公司资料公开。
- 不提交原始素材目录、旧方案、构建缓存、凭证或个人电脑路径日志。运行必需的公司内容与许可声明保留。
- 变更完成运行 npm test 与 npm run build；实际操作回归、触摸模拟和接屏验收分别报告。
