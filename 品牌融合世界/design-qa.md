# 品牌探索模块视觉验收

- source visual truth: `docs/module-qa/approved-home.jpg`
- implementation screenshot: `docs/module-qa/final-home.jpg`
- viewport: 1280 × 720，中文首页，原版与新版都为 1 倍截图密度。
- source / implementation pixels: 均为 1280 × 720。
- full-view comparison evidence: `docs/module-qa/home-comparison.jpg`，两张等尺寸画面并排；已实际打开比较。
- focused region comparison evidence: `docs/module-qa/hero-comparison.jpg`，标题、按钮、留白和底部领域导航并排。

## 对比结论

已认可的首页构图、官方标志、深蓝氛围、宋体标题、产品主视觉位置与三领域入口保留。物件浮动角度的轻微变化来自实时动画，不是构图改动。导航改为品牌、品类、产品、合作属于用户新范围的有意修改。

## 必需保真面

- 字体与排版：首页字体、字重、行高、断行和按钮字号与基准一致。新增资料层使用同一字体系统；1280 英文案例完整可读，标题合理换行。
- 间距与布局：首屏标题、主按钮、底栏间距未漂移。图片详情以左侧大图和右侧说明呈现，支持图片内部缩放，持续操作不被遮挡。
- 颜色与样式：保留深蓝背景与香槟金交互色。品牌识别图增加浅色承托，改善深色品牌字标的辨识度。
- 图像质量与资产：继续使用本项目真实程序渲染素材与官方原始标志，没有替换品牌标志为绘制近似品。图片产品明确标注概念；高倍缩放受 1000 × 800 概念素材原始分辨率限制。
- 文案与内容：新增层次使用原始品牌、品类、官网关系和案例，未给独立概念绑定品牌。原表品牌记录与官网品牌来源分开。

## 修正历史

1. [P2，已修正] 品牌列表的深色字标在深蓝背景上不清晰。增加浅色标志承托与足够内边距。复核截图 `brand-final-1920.jpg` 中全部五个标志可辨识。
2. [P2，已修正] 图片详情背后仍露出三维展示牌边缘。切换图片/短片时隐藏产品三维物件，保留环境背景。复核 `image-touch-1280.png`，图片周围没有重叠物件边缘。
3. [P2，已修正] 品类搜索深入后缺少返回原搜索结果入口。加入搜索上下文保存并验证 Lip Stick → 细分类 → 返回搜索结果，搜索词与结果完整恢复。
4. 复核后的首页并排比较没有新增 P0/P1/P2 视觉偏差。新的品牌、图片与案例界面与用户接受的色彩和字体系统协调。

## 验证范围

- 1280 × 720：首页并排比较、图片详情、品牌/品类/产品导航和英文案例。
- 1920 × 1080：完整品牌资料层截图，未溢出；`brand-final-1920.jpg` 为 1920 × 1080。
- 3840 × 2160：实际页面尺寸与布局边界检查；主文档无溢出，关闭与品牌说明均位于视口内。应用内截图工具返回 3840 × 1959 裁切图，因此 `brand-final-3840.jpg` 只作为局部视觉证据，不冒充完整 4K 截图。
- 图片多点触摸：Chrome 模拟两触点，比例 1 → 1.933，随后单指平移 x +80、y +40。见 `touch-image.json`。不是实体大屏验收。
- 三维拖动、产品筛选、进入/返回、自动展示恢复原状态，见 `return-3d.json`。
- 最新独立预览浏览器日志没有 error / warn。5178 在另一个并行修改期间曾产生宿主场景错误；本次接入路径的进出与语言同步实际通过，不把该宿主整个项目算作本模块验收范围。

实体大屏驱动、现场性能与远距离文字观看仍需现场验收。高倍品牌标志展示可在后续获取官方更高分辨率源文件时提升清晰度。

final result: passed
